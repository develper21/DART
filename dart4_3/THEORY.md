# DART-4.3 Theory
A reusable primitive is only useful when the final algorithm explicitly
references it, binds its inputs, composes it, and verifies the result.
The verifier remains authoritative; the planner only proposes.
