# DART-3.5 Theory

The core hypothesis is that a reusable task program should be the compiled consequence of an invariant task law, not a direct fit to observed regimes.

Law fields:
- relation
- symmetry
- scale response
- translation/offset response

Pipeline:
A-D behavioral probes -> TaskLaw -> compiler -> frozen primitive + small program -> E extrapolation.
