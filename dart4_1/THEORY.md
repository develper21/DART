# DART-4.1 Theory

Target capability:

behavior -> primitive induction -> exact verification -> persistent library -> primitive reuse -> composition -> exact verification.

The decisive experiments are novel primitive discovery, primitive reuse on later hidden tasks, ternary composition, cross-seed stability, and explicit failure/anomaly reporting.
